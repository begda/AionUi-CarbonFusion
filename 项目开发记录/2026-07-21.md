# 2026-07-21 开发记录

------------------- 2026年 07月 21日 12：33：33增加

✅ 完成 carbonfusion 构建体系搭建：

- 新建专用 GitHub Actions 工作流 `.github/workflows/build-carbonfusion.yml`，仅构建 `macos-arm64` 和 `windows-x64`
- 注释掉 `macos-x64`、`linux-x64`、`windows-arm64`，保留配置可快速启用
- 工作流已提交并推送至远程

✅ 完成分支同步与更新：

- 添加 upstream 远程（`https://github.com/iOfficeAI/AionUi.git`）
- 更新 `main` 分支，合并上游最新代码（含 triage 规则、测试增强等）
- 将 `main` 合并至 `carbonfusion` 分支，确保自定义分支基于最新基础

✅ 构建验证：

- 推送测试 commit 触发 `build-carbonfusion.yml`，构建流程已就绪
- 当前构建耗时约 161 分钟/次，远低于 GitHub 免费额度（2000 分钟/月）

⚠️ 注意事项：

- `build-and-release.yml` 仍保留 `carbonfusion` 支持，但推荐优先使用独立工作流以避免冲突
- 后续如需新增平台（如 Linux），只需取消对应行的 `#` 注释即可
- ------------------- 2026年 07月 21日 16：15：07增加--------------------

✅ build-and-release.yml 复原：

- 已完全恢复为 upstream/main 原始版本，移除所有 carbonfusion 相关修改
- 确保与 iOfficeAI/AionUi 官方行为一致，避免未来合并冲突
- 当前构建职责完全由 build-carbonfusion.yml 承担，职责分离清晰
